[package]
name = "apex-edge-loyalty"
version = "0.1.0"
edition = "2021"
description = "Loyalty provider trait and local earn/redeem implementation for ApexEdge"

[dependencies]
thiserror = "1.0"
uuid = { version = "1.0", features = ["v4", "serde"] }
